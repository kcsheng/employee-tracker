{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "This app provides an interface that allows the user to interact with the relational database pertaining the department, role and employee. The app is built upon npm packages including mysql, inquirer, console.table and dotenv.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0",
    "dotenv": "^10.0.0",
    "inquirer": "^8.1.2",
    "mysql2": "^2.3.0",
    "npm": "^7.20.5"
  }
}
